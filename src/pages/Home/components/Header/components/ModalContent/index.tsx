import Steps from 'components/Steps';

import { Container } from './styles';

const ModalContent = () => {
  return (
    <Container>
      <Steps step={1} />
    </Container>
  );
};

export default ModalContent;
